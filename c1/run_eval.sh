#!/bin/bash
cd /root/.nyq/graduate

# ========================================================
# 🛠️ 动态定位 Conda (无论安装在哪里都能找到)
# ========================================================
# 1. 询问 conda 可执行文件它的安装根目录在哪里
CONDA_BASE=$(conda info --base)

# 2. 拼接出 conda.sh 的绝对路径并加载
source "$CONDA_BASE/etc/profile.d/conda.sh"
# ========================================================

conda activate mem

echo ">>> 运行评测..."
python c1/eval_stream_locomo.py